require 'spec_helper'

describe TextMasterShop do
  it 'has a version number' do
    expect(TextMasterShop::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
